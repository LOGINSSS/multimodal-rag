
# 来时路 — RAG 进阶知识笔记

> 记录日期：2026-08-13
> 关联项目：`RAG`（LangGraph + DeepSeek-R1 + Qwen-VL + DashScope + Milvus）
> 用途：记录本人在搭这套 RAG 过程中啃下来的概念，作为回头查阅的「来时路」。

---

## 〇、总览：这 5 个概念在你的管线里各在哪

```
用户 query
   │
   ▼
① R1 重构 query + ② HyDE（生成假设答案）   ── 查询增强层
   │
   ▼
③ 混合检索：BM25(稀疏) + 稠密向量 + CLIP 视觉向量（图片）  ── 召回层
   │        └── ① RRF：把多路结果融合成一张排序表
   ▼
④ Rerank 精排（Qwen3-Reranker）           ── 精排层
   │
   ▼
⑤ Qwen-VL 生成最终答案                     ── 生成层
   │
   ▼
⑥ Ragas 评估（离线，判断改得好不好）       ── 评估层
   │
   ▼
⑦ LangFuse 追踪（在线，看每次 trace）      ── 观测层
```

| 概念 | 位置 | 一句话 |
|------|------|--------|
| RRF | 检索层 | 把「分数不可比」的多路检索结果，按**名次**而不是分数融合 |
| HyDE | 查询增强 | 先让 LLM 编个假答案，拿假答案去检索，缩小「问题↔文档」的语义鸿沟 |
| CLIP | 检索层(图片) | 把图片和文字映射到**同一个向量空间**，直接「用文字搜图片」 |
| Ragas | 评估层 | 用 LLM 给你的 RAG 自动打分（检索准不准、答案忠不忠实） |
| LangFuse | 观测层 | 开源可观测平台，记录每次运行每个节点的输入/输出/token/耗时 |

---

## 一、RRF（Reciprocal Rank Fusion，倒数排名融合）

### 是什么
一种**融合多个排序结果**的算法。当你同时跑了多个检索（BM25 一路、稠密向量一路、可能还有 CLIP 一路），每一路都返回一个「按分数排序的文档列表」，但——

**问题**：BM25 的分数是 0~几十的稀疏分数，稠密向量是 0~1 的余弦相似度，量纲完全不同，**没法直接加权求和**。

**RRF 的解法**：不关心具体分数，只关心**名次**。

### 原理（公式）
```
RRF_score(d) = Σ  1 / (k + rank_i(d))
              i
```
- `d`：某个文档
- `i`：第 i 路检索
- `rank_i(d)`：文档 d 在第 i 路里的**排名**（第 1 名 = 0 或 1，取决于实现，Milvus 里是 1-based）
- `k`：平滑常数，**默认 60**，防止排第一的权重过大

**直观理解**：一个文档在越多路里排得靠前，融合分越高。第 1 名贡献 `1/(60+1)≈0.016`，第 2 名 `1/62≈0.0161`，差别很小——所以它「温和地」奖励多路共识，而不是让某一路的第 1 名一票定生死。

### 为什么用它
1. 不用归一化、不用调「BM25 权重多少、向量权重多少」这种玄学参数
2. 简单、稳定、效果实测好，是当前混合检索的**事实标准**
3. Milvus 2.5+ 原生支持，一行代码

### 在本项目怎么用（Milvus + pymilvus）

```python
from pymilvus import AnnSearchRequest, RRFRanker

# 一路：稠密向量检索（DashScope embedding 生成的向量）
dense_req = AnnSearchRequest(
    data=[dense_vec],              # 查询向量
    anns_field="dense_vector",     # 你 collection 里的稠密字段名
    param={"metric_type": "IP", "params": {}},
    limit=20,
)

# 一路：BM25 稀疏检索
sparse_req = AnnSearchRequest(
    data=[sparse_vec],             # BM25 生成的稀疏向量
    anns_field="sparse_vector",    # 你 collection 里的稀疏字段名
    param={"metric_type": "IP", "params": {}},
    limit=20,
)

# RRF 融合
res = client.hybrid_search(
    collection_name="rag_chunks",
    reqs=[dense_req, sparse_req],
    ranker=RRFRanker(k=60),        # k 就是那个平滑常数
    limit=10,                      # 最终返回 top-10
)
```

### 坑 / 注意
- 各路返回的 `limit` 可以设大一点（比如 20~50），再融合取 top-K，召回更全。
- 如果某一类信号（比如 BM25）明显更重要，可以用 `WeightedRanker`（加权）替代 RRF，但要手动调权重；**先 RRF 起步**。
- BM25 需要 Milvus 里建 **sparse vector 字段**，且要用 `BM25EmbeddingFunction` 先生成稀疏向量，这一步别漏。

---

## 二、HyDE（Hypothetical Document Embeddings，假设文档向量）

### 是什么
检索前先让 LLM 根据用户问题**编一段「假设的答案」**，然后用这段假答案的 embedding 去检索，而不是用原始问题的 embedding。

### 解决什么问题：语义鸿沟
用户问的是**问句**，文档里写的是**陈述**。两者在向量空间里可能离得很远：

- 用户 query 向量：「怎么治疗感冒？」（疑问语气、短）
- 文档里实际内容：「感冒治疗包括多休息、补充水分、服用解热镇痛药…」（陈述、长、术语）

直接用 query 向量检索，可能匹配不到这些陈述句。**HyDE 让 LLM 先把问句「翻译」成陈述句**：

> 用户问「怎么治疗感冒？」→ LLM 编假答案「治疗感冒可以多休息、多喝水、吃感冒药」→ 拿这句话的向量去检索 → 更容易命中真实的治疗文档。

> 出处：Gao et al., *Precise Zero-Shot Dense Retrieval without Relevance Labels*（2022）。

### 原理
```
query ──LLM──> 假答案(hypothetical doc) ──embedding──> 检索向量
```
把「问题 → 文档」的匹配，变成「(编出来的)文档 → 文档」的匹配，语义距离更近。

### 在本项目怎么用（LangGraph 加一个 node）

```python
def hyde_node(state: dict) -> dict:
    from langchain_deepseek import ChatDeepSeek
    llm = ChatDeepSeek(model="deepseek-reasoner")  # 用 R1 编假答案
    resp = llm.invoke(
        f"请针对下面的问题写一段简短的回答（不要求准确，只是用于检索）：\n{state['question']}"
    )
    state["hyde_text"] = resp.content
    return state
```

然后在检索节点里：
- **方案 A（纯 HyDE）**：`embedding(hyde_text)` 去检索
- **方案 B（更稳）**：`embedding(hyde_text)` 和 `embedding(原始query)` 两路都跑，用 **RRF** 融合——这就是为什么 HyDE 和 RRF 常一起出现

### 坑 / 注意
- 假答案如果编得太离谱，会引入噪声。所以**方案 B（和原 query 融合）比纯 HyDE 稳**，推荐直接用 B。
- 对「事实型、简短问题」效果好；对「模糊、开放式」问题增益有限。
- 多一次 LLM 调用 = 多一份延迟和成本，可以用 R1 这种便宜模型，或只在「召回结果太少」时触发。

---

## 三、CLIP 视觉向量

### 是什么
OpenAI 2021 年提出的**图文对比学习模型**，把图片和文字**映射到同一个向量空间**。核心能力：可以算「一张图片」和「一段文字」的相似度。

### 解决什么问题：文字描述搜图片有「损耗」
你的计划里「向量库存图片描述」——检索时是「query 文字 → 图片描述的向量」匹配。但描述是**别人（或小模型）替图片写的转述**，天然有信息损耗：

- 图片里有个「穿红裙子的女孩在树下」→ 描述可能只写了「一个人在户外」→ 你搜「红裙子」就搜不到。

**CLIP 直接存图片本身的视觉向量**，query 文字也映射到同一空间，直接「文字 ↔ 图片」比对，跳过描述这个中间商。

### 原理
CLIP 用 4 亿张「图片-文字对」训练，让「匹配的图文对」向量靠近、「不匹配的」拉远。训练完后：
- 图像编码器 `encode_image(img)` → 图片向量
- 文本编码器 `encode_text(text)` → 文字向量
- 两个向量在同一空间，直接算余弦相似度。

### 在本项目怎么用

```python
import torch
import open_clip
from PIL import Image

# 英文/通用：ViT-B-32 或 ViT-L-14；中文推荐用 Chinese-CLIP（见下）
model, _, preprocess = open_clip.create_model_and_transforms(
    "ViT-B-32", pretrained="laion2b_s34b_b79k"
)
tokenizer = open_clip.get_tokenizer("ViT-B-32")

# 入库时：图片 → 视觉向量
image = preprocess(Image.open("fig.png")).unsqueeze(0)
with torch.no_grad():
    img_vec = model.encode_image(image)        # 存进 Milvus 的 clip_vector 字段

# 检索时：query 文字 → 文本向量
text = tokenizer(["红裙子女孩的图"])
with torch.no_grad():
    text_vec = model.encode_text(text)         # 拿它去 Milvus 检索图片
```

### 中文注意
- 原版 CLIP 是英文为主，中文效果一般。中文场景用 **`OFA-Sys/chinese-clip-vit-base-patch16`**（Chinese-CLIP，阿里达摩院 + 清华）。
- 用 `cn_clip` 包或 HuggingFace `transformers` 加载 Chinese-CLIP。

### 在你的「多向量」策略里的定位
每张图建议存 **3 个向量**，各有分工：

| 向量 | 来源 | 擅长 |
|------|------|------|
| OCR 文本向量 | OCR 出的文字 → 文字 embedding | 搜图片里的**文字**（表格、标题、数字） |
| 描述向量 | VLM 生成的图片描述 → 文字 embedding | 搜图片的**语义** |
| CLIP 视觉向量 | CLIP 图像编码器 | 搜图片的**视觉内容**（物体、场景、风格） |

三路用 **RRF 融合**，图片命中率最高。

### 坑 / 注意
- CLIP 对「物体/场景」强，对「文档里的细文字/OCR」弱——所以不能只靠 CLIP，OCR 那路不能省。
- CLIP 向量维度固定（如 ViT-B/32 = 512 维），建 collection 时字段维度要对应。
- CLIP 模型本身要下载（几百 MB~GB），首次加载慢，可预加载到内存常驻。

---

## 四、Ragas（RAG Assessment）

### 是什么
一个**专门评估 RAG 的框架**，用 LLM 自动给你的检索和生成质量打分，代替人工标注。

### 解决什么问题：盲调
你调 chunk 大小、rerank 阈值、top-K、HyDE 开关时，怎么知道「改对了还是改错了」？没有指标就是**凭感觉盲调**。Ragas 给出一组可量化分数，让你每次改动有据可依。

### 核心指标（两大类）

**检索质量（Retrieval）**
- **Context Precision（上下文精确度）**：检索到的上下文里，相关的排得靠前吗？（排错位置扣分）
- **Context Recall（上下文召回率）**：标准答案需要的信息，检索到的上下文里都有吗？

**生成质量（Generation）**
- **Faithfulness（忠实度）**：生成的答案，**有没有编造检索上下文里没有的东西**？（反幻觉，最关键）
- **Answer Relevancy（答案相关性）**：答案有没有正面回答用户的问题？

> 可选：**Answer Correctness（正确性）**——需要标准答案，衡量答案和标准答案一致度。

### 在本项目怎么用（用 DeepSeek 当裁判）

```python
from ragas import evaluate
from ragas.metrics import (
    faithfulness,          # 忠实度
    answer_relevancy,      # 答案相关性
    context_precision,     # 上下文精确度
    context_recall,        # 上下文召回率
)
from langchain_deepseek import ChatDeepSeek

# 用 DeepSeek 当打分裁判（默认用 OpenAI，国内要换成 DeepSeek）
judge_llm = ChatDeepSeek(model="deepseek-chat")

# 准备测试集：问题 + 你的 RAG 的答案 + 检索到的上下文 + 标准答案
dataset = {
    "question":      ["问题1", "问题2", ...],
    "answer":        ["你的RAG生成答案1", ...],   # 实际跑出来的
    "contexts":      [["检索上下文1-1", "1-2"], ...],  # 检索返回的
    "ground_truth":  ["标准答案1", ...],           # 人工写/认可的
}

result = evaluate(
    dataset,
    metrics=[context_precision, context_recall, faithfulness, answer_relevancy],
    llm=judge_llm,
)
print(result)   # 每个指标一个分数（0~1 或 0~1 区间），越高越好
```

### 坑 / 注意
- **Ragas 的 API 迭代非常快**（指标名、传参方式经常变），上面是 0.2.x 的典型写法，**用之前务必 `pip show ragas` 看版本 + 查官方文档**。
- 需要准备一个 **10~50 条的测试集**（问题 + 标准答案），这是唯一要「花点人工」的部分，但一劳永逸。
- 裁判 LLM 本身也会错，分数是「相对参考」，用来**对比两次改动的趋势**，别当成绝对真理。
- 默认走 OpenAI 打分，国内要显式指定 `llm`（换成 DeepSeek）和 `embeddings`，否则会报网络错。

---

## 五、LangFuse

### 是什么
开源的 **LLM 应用可观测平台**（对标 LangSmith，但开源、可自托管）。自动记录每次运行的完整 trace：每个节点的输入/输出、token、耗时、成本、异常。

### 和 LangSmith 的区别
| | LangFuse | LangSmith |
|---|---|---|
| 开源 | ✅ 可 Docker 自托管 | ❌ 闭源 SaaS |
| 数据 | 可留在本机 | 上 LangChain 云 |
| 国内访问 | 自托管则无需代理 | 通常要代理 |
| 生态 | LangChain/LlamaIndex 都支持 | LangChain 官方 |

**对你的意义**：你之前问「怎么看到算子意图和 trace」，LangFuse 就是那个「有 UI、能点开每个节点看输入输出」的工具，而且能 Docker 自托管（你正好有 Docker Desktop），**数据不出本机**。

### 怎么用（两种方式）

**方式 A：官方云（最快）**
1. 到 cloud.langfuse.com 注册，项目设置里拿 `public key` + `secret key`

**方式 B：Docker 自托管（数据不出本机）**
1. 官方提供 docker-compose（含 postgres + clickhouse + minio + langfuse）
2. `docker compose up -d` 起来后，访问 `http://localhost:3000`

### 接入 LangGraph（代码）

```bash
# 1. 安装
uv add langfuse

# 2. 设环境变量（或 .env 里配）
export LANGFUSE_PUBLIC_KEY="pk-lf-..."
export LANGFUSE_SECRET_KEY="sk-lf-..."
export LANGFUSE_HOST="http://localhost:3000"   # 自托管；用云就填 https://cloud.langfuse.com
```

```python
# 3. 用 CallbackHandler 挂到 LangGraph
from langfuse.callback import CallbackHandler

langfuse_handler = CallbackHandler()

# graph.invoke / graph.stream 时传入
result = graph.invoke(
    inputs,
    config={"callbacks": [langfuse_handler]},
)
```

跑完去 LangFuse 的 Web UI，就能看到一棵 **trace 树**：`query重构` → `HyDE` → `混合检索` → `rerank` → `生成`，点每个节点看它的输入输出和耗时。

### 坑 / 注意
- 环境变量命名：`LANGFUSE_PUBLIC_KEY` / `LANGFUSE_SECRET_KEY` / `LANGFUSE_HOST`，别和 LangSmith 的 `LANGSMITH_*` 混。
- 自托管要开 postgres + clickhouse 两个库，Docker Desktop 资源（内存）要给够，否则起不来。
- LangFuse 和 LangSmith 二选一即可，别同时开（trace 会重复上报）。

---

## 六、一句话速查表

| 概念 | 一句话 | 什么时候用 |
|------|--------|-----------|
| **RRF** | 按名次融合多路检索，不调权重 | 你有 ≥2 路检索（BM25+向量 / 多向量图片） |
| **HyDE** | 让 LLM 编假答案，拿假答案去检索 | 召回率低、事实型短问题 |
| **CLIP** | 图文同一向量空间，文字直接搜图片 | 检索图片视觉内容（配合 OCR/描述） |
| **Ragas** | LLM 自动给 RAG 打分 | 调参/改架构后，对比效果好不好 |
| **LangFuse** | 开源可观测，记录每次运行 trace | 想看每一步的输入输出、排查线上问题 |

---

## 附：这些概念在 LangGraph 图里的伪代码骨架

```python
from langgraph.graph import StateGraph, START, END

def query_rewrite(state): ...      # R1 重构 query
def hyde(state): ...               # HyDE 编假答案
def retrieve(state): ...           # BM25 + 稠密 + CLIP，RRF 融合
def rerank(state): ...             # Qwen3-reranker 精排
def generate(state): ...           # Qwen-VL 生成（含图片 base64）

g = StateGraph(State)
g.add_node("query_rewrite", query_rewrite)
g.add_node("hyde", hyde)
g.add_node("retrieve", retrieve)
g.add_node("rerank", rerank)
g.add_node("generate", generate)

g.add_edge(START, "query_rewrite")
g.add_edge("query_rewrite", "hyde")
g.add_edge("hyde", "retrieve")
g.add_edge("retrieve", "rerank")
g.add_edge("rerank", "generate")
g.add_edge("generate", END)

graph = g.compile()

# 调试时看 trace：
for chunk in graph.stream({"question": "..."}, stream_mode="debug"):
    print(chunk)
```
